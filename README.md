# chat-socket.io-studyng

## Como rodar a aplicação:

Para rodar o frontend da aplicação, basta rodar o comando abaixo:

```bash
  cd client && npm start
```

Para rodar o servidor, basta rodar o comando abaixo:

```bash
  node server/index.js
```
